{
  "name": "b-pos-functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:emulator": "firebase --config ../firebase.json --project demo-b-pos emulators:exec --only firestore \"vitest run --config vitest.emulator.config.ts\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "@types/node": "^20.19.43",
    "firebase": "^11.10.0",
    "firebase-tools": "^15.30.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^6.6.0",
    "zod": "^3.25.76"
  }
}

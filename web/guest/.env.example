# 本機 npm run dev 才需要這一份，複製成 .env.local 再填。
#
# 部署到 Firebase Hosting 之後不需要：Hosting 會在 /__/firebase/init.json
# 自動提供所屬專案的設定，程式優先讀那個（見 src/firebase.ts）。
#
# .env.local 在 .gitignore 裡。**不要把真實的專案 ID 或 apiKey 填進這份範本**，
# 這是公開 repo（CLAUDE.md 第一節）。下面全是假值。
VITE_FIREBASE_API_KEY=AIzaSyFAKE-not-a-real-key-0000000000000
VITE_FIREBASE_PROJECT_ID=demo-b-pos
VITE_FIREBASE_AUTH_DOMAIN=demo-b-pos.firebaseapp.com
VITE_FIREBASE_APP_ID=1:000000000000:web:0000000000000000000000
